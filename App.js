import React from "react";
import { ReactDOM } from "react-dom";

const heading=React.createElement('h1',{style: {color: 'red'}},"My First Namaste React Code__!!!")
const root= ReactDOM.createRoot(document.getElementById('root'))
root.render(heading);
